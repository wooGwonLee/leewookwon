# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`wooGwonLee/leewookwon`) is currently empty — no commits, no source files, and no
project scaffolding exist yet on any branch (local or remote).

There are no build, lint, or test commands to document, and no architecture to describe, because no
code has been added. Once the project is scaffolded (e.g. `npm init`, `cargo new`, `create-react-app`,
etc.), this file should be updated to reflect:

- The language/framework chosen and how to install dependencies
- Commands to build, lint, and run tests (including running a single test)
- The high-level architecture and how major components fit together
